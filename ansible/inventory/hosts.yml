---
# Ansible inventory for on-premises OTel agent deployment
# Replace with actual host entries

all:
  vars:
    gateway_endpoint: "gateway.observability.internal:4317"
    environment: prod
    datacenter: dc1

  children:
    onprem_linux:
      hosts:
        # linux-app-01:
        #   ansible_host: 172.16.10.11
        #   service_name: order-service
        #   log_paths:
        #     - /var/log/app/*.log
        #     - /opt/order-service/logs/*.log
        # linux-app-02:
        #   ansible_host: 172.16.10.12
        #   service_name: inventory-service
      vars:
        ansible_user: deploy
        ansible_become: true

    onprem_windows:
      hosts:
        # win-app-01:
        #   ansible_host: 172.16.10.21
        #   service_name: legacy-billing
        #   enable_iis: true
        #   log_paths:
        #     - 'C:\logs\billing\*.log'
        # win-app-02:
        #   ansible_host: 172.16.10.22
        #   service_name: legacy-reporting
        #   enable_iis: true
      vars:
        ansible_connection: winrm
        ansible_winrm_transport: kerberos
        ansible_winrm_server_cert_validation: ignore
